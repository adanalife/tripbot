name: Docker
on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ENV: test
    #   PG_PASS: ${{ secrets.PG_PASS }}
    #   DATABASE_URL: postgresql://postgres:${{ secrets.SECRET_PASSWORD }}@postgres/postgres
    #   SECRET_KEY: ${{ secrets.SECRET_KEY }}
    #   PORT: 8080

    steps:
    - name: Checkout (without LFS)
      uses: actions/checkout@v1
    - name: Fetch videos from cache
      uses: actions/cache@v1
      with:
        path: assets/video
        key: ${{ runner.os }}-video-${{ hashFiles('assets/video/*.MP4') }}
        restore-keys: |
          ${{ runner.os }}-video-
    - name: Checkout (with LFS)
    # use git-lfs to download big video file(s)
    - uses: actions/checkout@v1
      with:
        lfs: true
    - name: Set ENV
      run: echo "ENV=${ENV}" >> cmd/tripbot/env.docker
    - name: Build the docker-compose stack
      run: docker-compose --project-directory . -f cmd/tripbot/docker-compose.yml up -d
    - name: Check running containers
      run: docker ps -a
    - name: Check logs
      run: docker logs danalol-stream_tripbot_1
    # - name: Run test suite
    #   run: docker exec backend pytest --skip-auth
