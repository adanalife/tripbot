[mypy]
# Standalone scripts in bin/ have hyphenated filenames (e.g. obs-media-restart)
# which aren't valid Python module names and all get treated as __main__,
# colliding when more than one exists. They're glue for the OBS WebSocket,
# not part of any importable package — skip type-checking here.
exclude = ^bin/
