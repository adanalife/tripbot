version: '3.7'

services:
  tripbot:
    environment:
      ENV: development
    env_file:
      - ./.env.development
    command: bash -c go run cmd/tripbot/tripbot.go
    # entrypoint: ["go", "run", "cmd/tripbot/tripbot.go"]

  obs:
    environment:
      ENV: development
    env_file:
      - ./.env.development

  db:
    volumes:
      - ./tmp/data:/var/lib/postgresql/data
