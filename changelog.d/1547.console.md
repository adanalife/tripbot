The console's audio panel and the album chat commands no longer re-read the music index from disk on every request: it is cached behind the index file's mtime, so newly staged albums still appear without a restart.
