Drop `ONSCREENS_SERVER_RUN_DIR` and the emptyDir mounted for it — nothing has written to the run dir since the on-disk state files were removed, so the boot-time `MkdirAll` was a startup failure mode guarding an empty directory.
