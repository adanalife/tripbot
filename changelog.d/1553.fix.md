Sentry events whose text describes the cluster rather than a defect — a neighbour pod refusing the connection, a name that did not resolve, a gateway answering 502 — now carry a `noise` tag naming the class, so `!noise:*` is the defect-only view of the issue list.
