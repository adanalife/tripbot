State crossings are now recorded at the moment the footage crosses the line, not at the next clip switch. A new ten-second tick reads the playhead and the per-moment track, so the 48 clips that cross a state mid-clip write their `state_crossing` event with the exact offset into the clip (`video_ts_sec`); clips without a track still answer at clip level, as before.
