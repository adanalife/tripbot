`frame_embeddings` now carries `source_ts_sec`, the offset into the original recording, alongside the existing offset into the airing clip. Re-cutting one of the 122 trimmed clips becomes arithmetic on `ts_sec` instead of a multi-day re-embed, and the dedupe key moves to the clock nothing re-cuts. Existing rows are backfilled from the trim offset each clip's `video_coords` track already implies.
