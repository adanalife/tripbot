`!skip` and `!back` now move by a span of footage instead of a clip count: `!skip 10m`, `!back 1h30m`, bare numbers meaning minutes, negatives flipping direction, any timescale (moves longer than the corpus wrap around) — and the reply states the time moved. Without an argument they still hop one clip.
