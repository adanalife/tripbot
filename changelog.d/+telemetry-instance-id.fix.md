Stamp `service.instance.id` on the telemetry resource, so each per-platform instance gets its own metric series. Metrics recorded without an explicit platform attribute — `tripbot_gateway_up`, `tripbot_obs_silent_disconnect_restarts_total` — were sharing one series across every instance and reporting whichever pod pushed last.
