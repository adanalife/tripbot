The playback chat commands (`!timewarp`, `!jump`, `!daytime`, `!skip`, `!back`, `!find`) now take their host check through an overridable seam, so their 22 tests run on macOS instead of skipping — and the no-playout branch they used to occupy locally has a test of its own.
