The testing workflow reports a failing suite as a failure. The test step piped `task test:cover` into `tee`, and the default shell doesn't set `pipefail`, so the step took `tee`'s exit status and a red suite reported green — it only ever went red when the coverage upload itself failed.
