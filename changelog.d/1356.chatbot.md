`!location` and the onscreen location rotator answer from the clip's own stored place name when the playhead's moment has none, so the clips whose per-moment track isn't trustworthy stop costing a live Google Maps lookup too.
