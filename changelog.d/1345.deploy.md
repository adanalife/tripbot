tripbot mounts the album library from the node-local `obs-music-local` claim instead of the NAS share, so a storage outage can't block the goroutine that enumerates and advances tracks. Same library, same mount path — infra provisions the volume and `task k8s:<env>:music-localize` fills it.
