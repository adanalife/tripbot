Retire the `c.Conf` config globals: `pkg/config/tripbot` and `pkg/config/onscreens-server` drop their `init()` loaders for a `Load()` called once from main, and config is threaded through the servers, rotators, and command handlers instead of read from a package global — final step of retiring config-as-global.
