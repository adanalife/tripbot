Count each time the audio watchdog re-reads the live background-audio bed off OBS (`tripbot_background_audio_resyncs_total`), so an OBS restart's bed recovery is visible as a series rather than only as a log line.
