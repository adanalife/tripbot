**Prod tripbot-twitch is wired to the platform-gateway.** `prod-1` points `TWITCH_API_URL` at its in-namespace `gateway-twitch` Service, mirroring the stage setup proven since 2026-06-19. Wired-but-dormant: Helix calls stay in-process until the `chatbot.twitch_gateway` flag (seeded off by migration 021) is toggled on from the console, and toggling it off is the instant, restart-free revert. Requires the `gateway-twitch` prod instance (with its Twitch tokens) to be live in `prod-1` before flipping the flag.
