`GET /api/insights/presence` ranks accounts by connected time over the last week — presence hours, sessions, longest session, and their `is_bot` / `exclude_from_leaderboard` flags — and `POST /api/user/{username}/flags` flips either flag, so an idle account farming the leaderboard can be found and taken off it.
