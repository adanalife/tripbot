TikTok platform support: a `PLATFORM=tiktok` instance polls inbound LIVE chat from gateway-tiktok and runs the v1 command allowlist. TikTok has no chat-post API, so command responses reach viewers via onscreens/playback effects only.
