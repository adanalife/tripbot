Instagram platform support: a `PLATFORM=instagram` instance polls inbound live-broadcast comments from gateway-instagram and runs the v1 command allowlist. The Graph API cannot post IG comments, so command responses reach viewers via onscreens/playback effects only.
