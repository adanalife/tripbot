Schema-validate the committed cdk8s `dist/*.k8s.yaml` against the k8s API with kubeconform, in both `task cdk8s:validate` and the `cdk8s-synth` CI gate — mirrors [infra#1007](https://github.com/adanalife/infra/pull/1007).
