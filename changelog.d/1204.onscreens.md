The TikTok corner rotators advertise gifting — the one interaction a viewer wouldn't guess works there — instead of pointing at Twitch. The Twitch calls-to-action are gone from the promo pools on every platform that uses them. The pools stay free of `!command` hints: anyone who knows the commands doesn't need a corner spent telling them.
