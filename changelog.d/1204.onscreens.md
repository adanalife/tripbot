The TikTok corner rotators advertise what a viewer can do there — send a gift, `!timewarp`, `!find <something>` — instead of pointing at Twitch. The Twitch calls-to-action are gone from the promo pools on every platform that uses them. A promoMode corner may now surface an effect command (one whose result lands on the stream, needing no chat reply) while still excluding reply-only commands.
