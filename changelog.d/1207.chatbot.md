`!find` and `!weather` are no longer feature-flagged. Both shipped behind a dormant gate while their dependencies were being proven; both are stable, so the gates and their `feature_flags` rows are gone. `!find` now answers on every gateway platform it's allowlisted on rather than only where a flag row happened to be enabled.
