An errored live-check no longer clears the silent-disconnect watchdog's collected misses. A check failing every other tick could hold the count under the threshold indefinitely, so a dark channel would never be recovered; misses are now held and aged out instead.
