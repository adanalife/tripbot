tripbot reads the currently-playing clip from playout (`playout-<platform>:8080`) instead of vlc-server — same `/vlc/current` wire contract, new server. Fixes the year-0001 rotator dates on platforms where vlc-server is scaled down.
