Remove the long-disabled state-flag overlay (the `!flag` command and the `ShowFlag` no-op that published nothing). `!state`/`!guess`/`!jump` keep their chat and playback behavior; they just no longer fire the dead overlay.
