A chatter whose platform id already sits on another `users` row — the shape a rename leaves behind — no longer re-issues the failing write on every message, and a rejected write no longer leaves the in-memory user claiming an id the database never took.
