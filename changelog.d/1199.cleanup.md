Removed the `bin/devenv` and `bin/prodenv` docker-compose wrappers (and the development/production compose overlays, plus the now-orphaned `onscreens-server` and `seed` services from the base compose). The full local stack runs on the k3d dev cluster now, and the `task test*` targets invoke `docker compose run --rm test` directly against the testing overlay — the same stack CI uses.
