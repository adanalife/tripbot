Two minutes after startup tripbot counts the previous run's orphaned sessions — logins with no logout sharing their `session_id` — and reports them as the `tripbot_orphaned_sessions` gauge and a log line, so an ungraceful exit names how many viewers had in-flight miles discarded instead of waiting for a viewer to notice.
