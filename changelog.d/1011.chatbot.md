Monthly scoreboards are platform-scoped (`UNIQUE(name, platform)`, reads + creates filter by `STREAM_PLATFORM`), and `scores` gains the `UNIQUE(user_id, scoreboard_id)` constraint after merging historical duplicate pairs
