Retire the config-as-global pattern: the tripbot and onscreens-server `Conf` package globals and their `init()` loaders are gone — `config.Load()` runs once in main and the result is passed into every constructor.
