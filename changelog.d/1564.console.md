The `video.changed` envelope carries the van's `heading` and `speed_mps` at the playhead, read off the per-moment coordinate track, so the console can point its map marker the way the van is driving.
