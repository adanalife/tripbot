The tripbot and onscreens-server HTTP ports now come from `pkg/contract` in both the manifests and the binaries, rather than from literals spelled in each place. Same values — nothing changes at runtime.
