The staticcheck / unused / usetesting / thelper / tparallel lint job now fails on any finding, repo-wide, instead of reporting and passing. `unused` is newly enabled — golangci-lint splits it out from staticcheck, so dead code was previously unguarded.
